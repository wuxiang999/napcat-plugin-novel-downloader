# 贡献指南

感谢你考虑为 NapCat 小说下载插件做出贡献！

## 🤝 如何贡献

### 报告 Bug

如果你发现了 Bug，请：

1. 在 [Issues](https://github.com/wuxiang999/napcat-plugin-novel-downloader/issues) 中搜索是否已有相关问题
2. 如果没有，创建一个新的 Issue，包含：
   - 清晰的标题和描述
   - 复现步骤
   - 预期行为和实际行为
   - 环境信息（NapCat 版本、操作系统等）
   - 相关日志或截图

### 提出新功能

如果你有新功能建议：

1. 在 Issues 中创建一个 Feature Request
2. 详细描述功能需求和使用场景
3. 如果可能，提供实现思路

### 提交代码

1. **Fork 项目**
   ```bash
   git clone https://github.com/wuxiang999/napcat-plugin-novel-downloader.git
   cd napcat-plugin-novel-downloader
   ```

2. **创建分支**
   ```bash
   git checkout -b feature/your-feature-name
   # 或
   git checkout -b fix/your-bug-fix
   ```

3. **开发和测试**
   ```bash
   npm install
   npm run dev
   ```

4. **提交代码**
   ```bash
   git add .
   git commit -m "feat: 添加新功能"
   # 或
   git commit -m "fix: 修复某个问题"
   ```

5. **推送到 GitHub**
   ```bash
   git push origin feature/your-feature-name
   ```

6. **创建 Pull Request**
   - 在 GitHub 上创建 PR
   - 填写 PR 模板
   - 等待代码审查

## 📝 代码规范

### Commit 消息格式

使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
<type>(<scope>): <subject>

<body>

<footer>
```

类型（type）：
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 代码重构
- `test`: 测试相关
- `chore`: 构建/工具相关

示例：
```
feat(downloader): 添加断点续传功能

实现了下载中断后的断点续传功能，提升用户体验。

Closes #123
```

### 代码风格

- 使用 TypeScript
- 遵循 ESLint 规则
- 添加必要的注释
- 保持代码简洁清晰

### 文件结构

```
src/
├── config/          # 配置相关
├── core/            # 核心功能
├── handlers/        # 消息处理
├── services/        # 业务服务
├── types/           # 类型定义
└── utils/           # 工具函数
```

## 🧪 测试

在提交 PR 前，请确保：

- [ ] 代码可以正常编译
- [ ] 功能测试通过
- [ ] 没有引入新的 Bug
- [ ] 更新了相关文档

## 📚 文档

如果你的改动涉及：

- 新功能：更新 README.md 和 USAGE.md
- API 变更：更新相关文档
- 配置项：更新配置说明

## 🎯 开发建议

### 本地开发

```bash
# 安装依赖
npm install

# 开发模式（自动重新编译）
npm run dev

# 类型检查
npm run typecheck

# 构建
npm run build
```

### 调试

1. 在配置中开启 `debug: true`
2. 查看 NapCat 日志输出
3. 使用 `ctx.logger.debug()` 输出调试信息

## ❓ 需要帮助？

- 查看 [使用文档](./USAGE.md)
- 在 [Issues](https://github.com/wuxiang999/napcat-plugin-novel-downloader/issues) 中提问
- 加入讨论群（待建立）

## 📜 行为准则

- 尊重他人
- 保持友善和专业
- 接受建设性批评
- 关注对社区最有利的事情

---

再次感谢你的贡献！🎉
